export const initalState = {
    token:"spotify token added later",
};

const reducer = (state, action) => {
    switch(action.type) {
        default:
            return state;
    }
};

export default reducer;